{
  "name": "knotxme",
  "version": "1.0.0",
  "description": "Knotxme — full-stack platform (static frontend + Node/Express/MongoDB backend)",
  "private": true,
  "scripts": {
    "install:backend": "cd backend && npm install",
    "dev:backend": "cd backend && npm run dev",
    "start:backend": "cd backend && npm start",
    "dev:frontend": "npx http-server frontend/public -p 8080 -c-1",
    "dev": "npx concurrently -n BACKEND,FRONTEND -c blue,green \"npm run dev:backend\" \"npm run dev:frontend\"",
    "create-admin": "cd backend && npm run create-admin"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "http-server": "^14.1.1"
  }
}
